title: "TAKES_DB"
lang: ja
theme: pages-themes/midnight@v0.2.0

# 👇 これを追記：すべてのマークダウンファイルに自動で default レイアウトを適用する
defaults:
  - scope:
      path: "" # リポジトリ内のすべてのファイルを対象にする
    values:
      layout: "default"
